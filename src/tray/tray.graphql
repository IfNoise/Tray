type Tray {
  id: ID
  userId: String!
  plants: [String!]!
}

input AddPlantInput {
  plants: [String!]!
}

type Query {
  tray: [Tray]
}

type Mutation {
  addPlant(addPlant: AddPlantInput!): Tray
  cleanTray: Tray
}
